FROM oven/bun:debian AS deps
WORKDIR /app
COPY package.json ./
RUN bun install --production

FROM node:22-slim
WORKDIR /app
ENV NODE_ENV=production

COPY --from=deps /app/node_modules ./node_modules
COPY scripts/audit-worker.mjs ./scripts/audit-worker.mjs

CMD ["node", "scripts/audit-worker.mjs"]
